name = "yourpost-webmail"
compatibility_date = "2024-01-01"

# Static site - no Workers functions
# This tells Cloudflare Pages to treat this as a static site

[build]
command = "npm run build"
cwd = "."
watch_dir = "."

[dev]
port = 3000
local_protocol = "http"
ip = "0.0.0.0"
